# TODO - fast-burgerz fixes

## Step 1 (planned): Global mobile scroll fix
- [ ] Update `src/App.tsx` to remove `overflow-hidden` that can block vertical scrolling on mobile.

## Step 2 (planned): Prevent fixed UI overlap
- [ ] Update `src/App.tsx` to add safe bottom padding/margins so the floating WhatsApp button + toast don’t cover content.

## Step 3 (planned): Tracker responsiveness improvements
- [ ] Update `src/components/OrderTracker.tsx` progress bar + step nodes layout for narrow screens.

## Step 4 (planned): Verify build/live
- [ ] Run `npm run lint`
- [ ] Run `npm run build`
- [ ] Run `npm run preview` (optional for visual check)

